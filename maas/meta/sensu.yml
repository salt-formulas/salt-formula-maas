# Sample check
check:
  local_maas_proc:
    command: "PATH=$PATH:/usr/lib64/nagios/plugins:/usr/lib/nagios/plugins check_procs -C maas -u maas -c 1:1"
    interval: 60
    occurrences: 1
    subscribers:
    - local-maas-server
